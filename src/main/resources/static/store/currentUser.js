const userprofile = document.querySelector(".userprofile");

export const currentUser = {
    id: userprofile.dataset.userId
};

export const user_conversation_id = {
    conversation_id: userprofile.dataset.conversationId
};